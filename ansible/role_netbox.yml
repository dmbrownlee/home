---
- name: Ensure netbox servers are configured
  hosts: netbox
  vars_files:
    - "{{ lookup('env','HOME') }}/.site_data"
  remote_user: "{{ provisioning.username }}"
  become: true
  roles:
    - netbox
