---
- name: Ensure mirror hosts are configured
  hosts: mirrors
  vars_files:
    - "{{ lookup('env','HOME') }}/.site_data"
  remote_user: "{{ provisioning.username }}"
  become: true
  roles:
    - mirror
